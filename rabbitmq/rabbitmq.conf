## Импорт топологии (vhosts, users, permissions, exchanges, queues, bindings)
## из смонтированного файла при каждом старте ноды.
definitions.import_backend = local_filesystem
definitions.local.path = /etc/rabbitmq/definitions.json

## log.console уже включён образом в conf.d/10-defaults.conf
